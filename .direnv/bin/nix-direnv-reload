#!/usr/bin/env bash
set -e
if [[ ! -d "/home/nguyenhieu/projects/local/hpc_dispatch" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/nguyenhieu/projects/local/hpc_dispatch")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/nguyenhieu/projects/local/hpc_dispatch" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/nguyenhieu/projects/local/hpc_dispatch/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/nguyenhieu/projects/local/hpc_dispatch/.envrc" "/home/nguyenhieu/projects/local/hpc_dispatch/.direnv"/*.rc
